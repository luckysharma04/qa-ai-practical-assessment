# Shared test data for manual and automation teams.

| File | Purpose |
|------|---------|
| `users.json` | Default public SUT accounts |
| `billing.json` | Valid billing for checkout and API invoice |
| `invoice-payload.example.json` | Reference POST /invoices body from assessment |

Automation also uses `PrismStructure/Data/` and `PrismStructure/Utils/dataGenerator.js`.
