{
  "name": "toolshop-prism-playwright",
  "version": "1.0.0",
  "description": "Playwright Prism Framework for Practice Software Testing Toolshop",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @Smoke",
    "test:regression": "playwright test --grep @Regression",
    "test:ui": "playwright test --grep @UI",
    "test:api": "playwright test --grep @API",
    "test:ui:smoke": "playwright test --project ui-smoke",
    "test:ui:regression": "playwright test --project ui-regression",
    "test:api:smoke": "playwright test --project api-smoke",
    "test:api:regression": "playwright test --project api-regression",
    "report": "playwright show-report Reports/playwright-report"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.0",
    "@playwright/test": "^1.49.0"
  }
}
